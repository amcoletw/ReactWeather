var React = require('react');

var About = (props) => {
  //use when we're not managing state
  return(
    <h3>About component</h3>
    <p>Welcome to the about page!</p>
    )
};

module.exports = About;

